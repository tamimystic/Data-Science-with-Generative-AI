# we want to see that,how much library in our env -> if we can not see   ->(conda install pip --force-reinstall)->(pip list)

# conda create -n <environment_name> python=<python_version> -y

# conda env list

# conda activate <environment_name>

# conda deactivate

# for library version go to there (https://pypi.org/) and search package name and get package version

# pip install pandas==2.3.2 (package install)
# pip uninstall pandas (package uninstall)

# conda remove --name <environment_name> --all








# for specific folder
# conda create --prefix .\<environment_name> python=<python_version> -y

# conda activate .\<environment_name>

# here all process/command are same

